<section class="block-image-text-section {% if is_gray_background %} gray {% endif %}">
    <div class="container">
        <div class="block-image-text-row">
            <div class="image-col">
                <img src="{{ image.src }}" alt="{{ image.alt|e }}">
            </div>
            <div class="content-col">
                {{ content|e('wp_kses_post') }} 
            </div>
        </div>
    </div>
</section>